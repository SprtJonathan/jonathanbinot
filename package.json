{
  "name": "vite-project",
  "private": true,
  "version": "0.0.4",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "npm install -g vite && npm install && vite --host --port=80"
  },
  "devDependencies": {
    "sass": "^1.53.0",
    "vite": "^3.1.4"
  },
  "dependencies": {
    "vite": "^3.1.4"
  }
}
